export default function Navbar() {
    return (
        <header>
            <nav className="header">
                <img className="logo" src="/logo.png" alt="React logo" />
                <span className="title">ReactFacts</span>
            </nav>
        </header>
    );
}
